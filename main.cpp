#include "opencv2/opencv.hpp"
#include "common.h"
#include "slider.h"
#include <cmath>
#include <fstream>
#include <ranges>
#include <functional>
#include <tuple>

#include "spaces.h"

int main() {
  Logger::init();

  INFO("Built! Now running!");

  Logger::destroy();
  return 0;
}
